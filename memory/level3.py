# Minimal stub — full implementation in Task 14
def should_update(completed_game_count: int) -> bool:
    return completed_game_count > 0 and completed_game_count % 5 == 0


def update_profile(l2_reports):
    pass
